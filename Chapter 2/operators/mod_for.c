/* modify the loop
 * for (i=0; i<lim-1 && (c=getchar()) != '\n' && c != EOF; i++)
 * such that no &&s or ||s are used
 */

#include <stdio.h>


for (i=0; i<lim-1; i++)
/* nvm, they just want a series of if else statements */
