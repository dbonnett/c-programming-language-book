#include <stdio.h>
#include <limits.h>
#include <float.h>

int main() {
	printf("long max: %ld",LONG_MAX);
	printf("char max: %c",CHAR_MAX);
	printf("uint max: %u",UINT_MAX);
	printf("flt max: %f",FLT_MAX);
}
